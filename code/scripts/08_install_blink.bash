#!/bin/bash

# Installs dependencies for blink.sh and `pinout`
# pinout shows some information about the pi pinout in the console
# blink blinks the leds to help find your pi

require pinout python3-gpiozero
